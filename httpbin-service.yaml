apiVersion: v1
kind: Service
metadata:
  name: web_name-admin-service
  annotations:
    cloud.google.com/app-protocols: '{"service-https": "HTTPS"}'
spec:
  selector:
    app: web_name-admin
  ports:
  - name: service-https
    port: 4200
    targetPort: 8080
  type: NodePort
